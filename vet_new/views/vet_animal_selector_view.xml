<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_vet_animal_selector_form" model="ir.ui.view">
        <field name="name">vet.animal.selector.form</field>
        <field name="model">vet.animal.selector</field>
        <field name="arch" type="xml">
            <form string="Select Animals by Owner">
                <group>
                    <field name="owner_id" options="{'no_create': True}"/>
                    <field name="contact_number"/>
                    <field name="animal_ids" widget="many2many_tags"/>
                </group>
                <footer>
                    <button name="action_confirm" string="Confirm" type="object" class="btn-primary"/>
                    <button string="Cancel" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_vet_animal_selector" model="ir.actions.act_window">
        <field name="name">Select Animals</field>
        <field name="res_model">vet.animal.selector</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
        <field name="view_id" ref="view_vet_animal_selector_form"/>
    </record>
</odoo>